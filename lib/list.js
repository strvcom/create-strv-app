'use strict'

// const path = require('path')
// const fs = require('fs-extra')

module.exports = async () => {
  // const rootPath = path.resolve(__dirname, 'templates')
  // const dir = await fs.readdir(rootPath)
  // const appTypes = dir.filter(type => type !== 'dotfiles')
}
