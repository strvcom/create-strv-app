import 'global/includes'
import React from 'react'
// import configureTheme from 'packages/configureTheme'
// import theme from 'common/theme'

const withRoot = ComposedComponent => () => <ComposedComponent />

export default withRoot

// todo implement styled-components:
// const { ThemeProvider } = configureTheme(theme)
